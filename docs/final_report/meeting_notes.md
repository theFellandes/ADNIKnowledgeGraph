Meeting Notes:
For the other ones (contributions) -> 7th one is the full contribution
C1 - FAIR and Semantic density are the methods.
	a) Use the pre-enrichment CauAD graph as the baseline and report before/after per step. Conceptually the cleanest, but the "before" already has some ontology codes on a few node types, so it is not a true zero. Method
C3 - Reproducibility mapping to show originality. (Show the mapping in detail).
C4 - Removed
C5 - It's just a step in the process not contribution (Check the literature whether normalization is considered a contribution)
C7 - After we complete this one, we can have a paper

C6 - After C7, we can have this one. Future work (We will work on it later. After June)

Finish with C7, submit the paper, after we can return it to C6.

Finish the master's thesis with metrics, change the acronym. Work with causality in the next step.

## Contributions List
C2 - C3 can be called steps
C1, C2 (more reproducable), C3 (name change: column-to-concept mapping), C7

C7 is the main contribution.

Diagram for contributions can be redrawn.

Detailed description for each section.

Detail the experimental section, experimental setup, methodologies, metrics.

Give some idea about experimental section, how our experimental results will be presented.

Title - Change it:
Ontology Selection and Enrichment for a Multimodal
Alzheimer’s Disease Knowledge Graph
(Remove enrichment)
(We should add Evaluation or Metrics something like that.)

